import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div``;

export default class VacationTable extends React.Component {
  render() {
    return <Wrapper></Wrapper>;
  }
}
